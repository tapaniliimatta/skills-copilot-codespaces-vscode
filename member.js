function skillsMember()
{
    // Create a new member object
    var member = new Member();

    // Set the member's skills
    member.setSkills(["JavaScript", "HTML", "CSS"]);

    // Display the member's skills
    member.displaySkills();
}